## Sorted List
### Our add is going to be O(n)
### Removing
remove *Array*
	index O(n)
	value O(logn) *if not found* , O(logn+n) (O(n)) *found*
	contains O(n)

remove *LinkedList*
	index O(n)
	value O(n)
	contains O(logn)

### We can make stuff with this
# PriorityQueue
- When we talk about priority we talk about

# Set
- Unordered
- Unique